import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  container: {
    position: "relative",
    left: 0,
  },
  image: {
    cursor: "pointer",
    width: "6rem",
    height: "6rem",
    marginBottom: "1rem",
  },
}));
